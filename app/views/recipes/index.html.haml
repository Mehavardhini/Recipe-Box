%h3 Recipes

- @recipes.each_slice(3) do |recipes|
  .row
    - recipes.each do |recipe|
      .col-md-4
        .recipe
          .image_wrapper
            = link_to recipe do
              = image_tag recipe.image.url, :class => "medium_img"
            %h2= link_to recipe.title, recipe
